# Quickstart: [FEATURE NAME]

**Plan:** `./plan.md` · **Created:** [YYYY-MM-DD]

The shortest path from a clean checkout to the feature working. Every command must be
copy-pasteable. Every step must state how you know it worked.

## Prerequisites

| Tool | Version | Check |
|---|---|---|
| [Tool] | [>= x.y] | `[command --version]` |

## 1. Install

```bash
[command]
```

**Expect:** [The observable result.]

## 2. Configure

```bash
cp .env.example .env
```

| Variable | Required | Meaning |
|---|---|---|
| `[VAR]` | yes | [Meaning. Say where to get the value.] |

Do not commit `.env`.

## 3. Run

```bash
[command]
```

**Expect:** [The observable result, including the URL or the log line.]

## 4. Verify

```bash
[test command]
```

**Expect:** [Pass count, or the exact output.]

## 5. Try the feature

1. [Action.] → [Observable result.]
2. [Action.] → [Observable result.]

## Troubleshooting

| Symptom | Cause | Fix |
|---|---|---|
| [Error message.] | [Cause.] | [Fix.] |

## Clean up

```bash
[command]
```
